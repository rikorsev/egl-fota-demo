
if(CMAKE_BUILD_TYPE STREQUAL "release")
    set(CONFIG_EGL_LOG_ENABLED 0)
    set(CONFIG_EGL_LOG_COLOR_ENABLED 0)
    set(CONFIG_EGL_RESULT_CHECK_ENABLED 0)
    set(CONFIG_EGL_RESULT_ERROR_HOOK_ENABLED 0)
else()
    set(CONFIG_EGL_LOG_ENABLED 1)
    set(CONFIG_EGL_LOG_COLOR_ENABLED 1)
    set(CONFIG_EGL_RESULT_CHECK_ENABLED 1)
    set(CONFIG_EGL_RESULT_ERROR_HOOK_ENABLED 1)
endif()

if(CONFIG_EGL_LOG_ENABLED EQUAL 1)
    add_definitions(-DCONFIG_EGL_LOG_ENABLED=1)
else()
    add_definitions(-DCONFIG_EGL_LOG_ENABLED=0)
endif()

if(CONFIG_EGL_RESULT_CHECK_ENABLED EQUAL 1)
    add_definitions(-DCONFIG_EGL_RESULT_CHECK_ENABLED=1)
else()
    add_definitions(-DCONFIG_EGL_RESULT_CHECK_ENABLED=0)
endif()

if(CONFIG_EGL_RESULT_ERROR_HOOK_ENABLED EQUAL 1)
    add_definitions(-DCONFIG_EGL_RESULT_ERROR_HOOK_ENABLED=1)
else()
    add_definitions(-DCONFIG_EGL_RESULT_ERROR_HOOK_ENABLED=0)
endif()

if(CONFIG_EGL_LOG_COLOR_ENABLED EQUAL 1)
    add_definitions(-DCONFIG_EGL_LOG_COLOR_ENABLED=1)
else()
    add_definitions(-DCONFIG_EGL_LOG_COLOR_ENABLED=0)
endif()

